// 4. Escreva uma nova versão do programa que usa uma estrutura de repetição for no
// lugar da estrutura while.

#include <stdio.h>
int main()
{
    int n = 10;
    for (int i = 1; i <= n; i++)
    {
        printf("%d \n", i);
    }
    return 0;
}